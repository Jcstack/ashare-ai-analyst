# Redis persistence configuration
# AOF: append-only file for durability (max 1s data loss)
appendonly yes
appendfsync everysec
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# RDB: periodic snapshots as backup
save 900 1
save 300 10
save 60 10000
